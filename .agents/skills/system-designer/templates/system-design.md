# {{项目名称}} 技术架构

> **状态**：{{草稿 / 评审中 / 已定稿}}
> **日期**：{{YYYY-MM-DD}}
> **基于**：{{项目名}} PRD + {{N}} 个 ADR
> **维护者**：{{作者}}

## 1. 概述

{{一段话说明这个项目是什么、采用什么架构风格、为什么。从 PRD 产品概述提炼 + ADR 架构决策综合而来，让读者不看其他文档就能理解系统级定位。}}

**业务背景**：
{{为什么做这个项目？业务痛点是什么？架构上要支持什么核心能力？}}

**架构风格**：
{{如"微服务架构，N 个核心服务 + 1 个 API 网关，服务间 gRPC 通信，每服务独立数据库 schema，部署在 Kubernetes"。}}

## 2. 技术选型

{{每行选型必须追溯到具体 ADR 编号。没有 ADR 支撑的选型 = 决策缺失，应先补 ADR 再写入本表。}}

| 类别 | 选型 | 说明 | ADR |
|---|---|---|---|
| **后端框架** | {{Micronaut / Spring Boot / ...}} | {{一句话理由：编译期注入、启动快、容器化友好}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **构建工具** | {{Gradle / Maven}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **数据访问** | {{Micronaut Data JDBC / JPA / MyBatis}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **数据库** | {{MariaDB / PostgreSQL / Cloud Spanner}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **服务间通信** | {{gRPC / REST}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **部署** | {{Kubernetes / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **定时任务** | {{xxl-job / Quartz / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **SSO / 鉴权** | {{公司 ALDP / 自建 OAuth2 / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **前端** | {{Vue 3 / React / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **消息队列**（若有） | {{Kafka / RabbitMQ / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| **缓存**（若有） | {{Redis / Caffeine / ...}} | {{一句话理由}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |

## 3. 架构图

{{画出整个系统的服务全景图——所有服务、调用关系、数据流向、协议。
用 mermaid graph（不用 ASCII art），体现"谁调谁、走什么协议、数据怎么流"。
这张图是系统的"地图"，读者看完就知道系统有哪些组成部分、怎么协作。}}

```mermaid
graph TD
    U[用户/浏览器] -->|{{HTTPS / REST + JWT}}| GW[{{API Gateway}}]
    GW -->|{{gRPC}}| S1[{{Service1}}]
    GW -->|{{gRPC}}| S2[{{Service2}}]
    S1 -->|{{JDBC}}| DB1[({{Database1}})]
    S2 -->|{{JDBC}}| DB2[({{Database2}})]
    SCHED[{{调度器}}] -->|{{触发}}| S1
    S1 -.->|{{gRPC（若跨服务调用）}}| S2
```

**图例说明**：
{{对图中的关键节点和连线做简要说明，如"实线=同步调用，虚线=定时/异步触发"。}}

## 4. 服务职责

{{列出系统所有服务，每个服务一行：服务名 + 核心职责 + 对外协议（给前端/外部）+ 对内协议（给其他服务/DB）。
只写职责边界，不写接口签名、不写校验规则——那些归 detail design。}}

| 服务 | 职责 | 对外协议 | 对内协议 |
|---|---|---|---|
| {{API Gateway}} | {{SSO 验证 + 请求路由}} | {{REST（用户友好）}} | {{gRPC → 后端服务}} |
| {{Service1}} | {{一句话核心职责}} | {{gRPC（只对 gateway）}} | {{JDBC → DB / gRPC → 其他服务}} |
| {{Service2}} | {{一句话核心职责}} | {{gRPC（只对 gateway）}} | {{JDBC → DB}} |

**服务划分依据**：
{{一段话说明为什么这么拆——按业务域？按变更频率？按团队边界？让读者理解拆分逻辑，而不是只看到结果。}}

## 5. 数据库划分

**原则**：Database-per-service——每服务独立 schema，服务间不跨库查表，需要对方数据走 gRPC。理由：{{如"避免服务间数据库耦合，独立演进，独立部署"}}。

### 5.1 {{service1}} schema

{{一段话说明这个库存什么——围绕这个服务的核心业务实体。}}

| 表 | 用途 |
|---|---|
| {{table1}} | {{一句话说明这张表存什么、关键约束（如"写入后不可变"）}} |
| {{table2}} | {{一句话用途}} |
| {{table3}} | {{一句话用途}} |

> 字段/索引/约束定义属 detail design，本表只列"表 + 一句话用途"。

### 5.2 {{service2}} schema

{{同上格式：一段话说明 + 表清单。}}

| 表 | 用途 |
|---|---|
| {{table1}} | {{一句话用途}} |
| {{table2}} | {{一句话用途}} |

## 6. 关键架构决策追溯

{{把架构文档里隐含的"为什么这么设计"显式列出来，每条追溯到 ADR。这是架构文档和 ADR 的衔接点——读者从这里能找到每个决策的完整论证。}}

| 决策 | 选择 | ADR |
|---|---|---|
| {{服务拆分粒度}} | {{微服务，按业务域拆}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| {{数据库隔离策略}} | {{Database-per-service}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| {{服务间通信}} | {{gRPC，类型安全}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |
| {{...}} | {{...}} | [ADR-000{{X}}](../docs/adr/000{{X}}-*.md) |

---

> **下游衔接**：本文档是 HLD 的强制上游。HLD（`<PROJECT>_HLD_*.md`）的"技术栈"小节应从本文档 §2 提取。
> 若架构变更，先更新本文档 → 再级联更新 HLD / detail design。
